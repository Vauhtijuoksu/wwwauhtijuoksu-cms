version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/home/cms
      - ./docker.sqlite3:/home/cms/db.sqlite3
    ports:
      - 8000:8000
