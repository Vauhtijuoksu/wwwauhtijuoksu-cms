apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-nginx
spec:
  type: ClusterIP
  selector:
    app: {{ .Chart.Name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: nginx
      name: http
